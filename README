# XV6 操作系统启动可视化实验报告

## 实验成果
成功在 Linux 环境下编译并启动 xv6，完成了启动流程的注释与可视化输出。

## 启动结果截图
![alt text](image.png)

## 难点攻克与技术说明
在完成 Task 3 (启动可视化) 时，遇到了 **Boot Sector Size Exceeded** (引导扇区体积超标) 的问题。
* **问题描述**：原计划使用 `cprintf` 打印完整字符串 `[BOOT] ...`，导致引导程序编译后体积为 540 字节，超过了 MBR 硬件规定的 512 字节限制。
* **解决方案**：采用了**极简字符可视化**方案。移除所有冗余字符串和换行符，直接操作串口端口 (`0x3f8`) 输出状态码。
    * 输出 **`1`**：代表 `Enter Bootmain` (进入引导主函数)
    * 输出 **`2`**：代表 `ELF Header Loaded` (ELF 头读取完毕)
    * 输出 **`3`**：代表 `Kernel Loaded` (内核加载完毕，准备跳转)
* **最终体积**：优化后引导扇区大小为 **483 字节**，成功通过编译。

## 个人总结
通过本次实验，深刻理解了操作系统引导过程中的硬件限制（512字节 MBR），以及从实模式切换到保护模式的完整流程...

---

# XV6 操作系统核心机制实践报告

## 实验内容说明
本次实践在上次启动流程分析的基础上，进一步深入操作系统的运行阶段，重点学习了系统调用、调度、内存管理及同步机制。完成的任务如下：

- **第一层：机制观察（必做）**
  - **任务 1：系统调用路径跟踪**：在 `syscall()` 和 `sys_write()` 中添加调试输出，并通过用户态程序 `trace_write.c` 验证调用路径。
  - **任务 2：调度过程观察**：在 `scheduler()` 中添加输出，记录进程切换时的 PID。
  - **任务 3：内存分配观察**：在 `kalloc()` 中记录物理页分配的起始地址。
- **第二层：机制理解（选做）**
  - **任务 1：系统调用扩展**：在内核中新增了 `hello()` 系统调用，从用户态添加接口，注册系统调用，并在 `hellotest.c` 中成功调用验证。
- **第三层：同步模型构建（挑战）**
  - **任务：简化版生产者–消费者模型**：在内核中新增了 `sys_produce()` 和 `sys_consume()` 两个系统调用，使用共享缓冲区（`int data[BUF_SIZE]`），并通过自旋锁（`spinlock`）保护临界区，同时结合了 `sleep()` 和 `wakeup()` 实现了阻塞与唤醒机制。

## 任务实现过程与截图
**第三层任务（生产者–消费者模型）实现过程**：
1. **定义结构**：在 `sysproc.c` 中定义了一个带有自旋锁和循环队列数据的全局结构体 `pc_buf`。
2. **实现 Produce**：`sys_produce` 获取锁后，如果缓冲区满，则调用 `sleep` 阻塞等待；否则写入数据，增加 `count`，然后调用 `wakeup` 唤醒可能在等待的消费者。
3. **实现 Consume**：`sys_consume` 获取锁后，如果缓冲区为空，则调用 `sleep` 阻塞等待；否则读取数据，减少 `count`，然后调用 `wakeup` 唤醒生产者。
4. **用户态验证**：编写 `prodcons.c`，使用 `fork()` 创建子进程。父进程调用 `produce()` 生产 5 个数据，子进程调用 `consume()` 消费 5 个数据。
*（此处可自行补充 QEMU 运行 `prodcons` 等程序的截图）*

## 遇到的问题及解决方法
1. **控制台输出风暴问题**：在跟踪 `sys_write` 时，由于 Shell 以及许多初始化进程都会频繁调用 write，导致控制台输出大量日志。
   - **解决方法**：通过在 `syscall()` 函数中判断 `num == SYS_write` 来精确捕获特定的系统调用；对于用户态，可以编写特定的测试程序来触发，区分系统自动调用和用户主动调用。
2. **生产者消费者模型的阻塞唤醒**：起初不确定在 xv6 中如何实现安全的阻塞机制。
   - **解决方法**：查阅了 xv6 内核中 `pipe.c` 的实现源码，参考了管道机制中的 `sleep()` 和 `wakeup()` 组合，以及配合 `spinlock` 的原子性释放与重新获取特性，成功实现了无死锁的同步模型。

## 实践心得
本次实践极大地加深了我对操作系统核心机制的理解。在跟踪系统调用的过程中，我真切地感受到了从用户态通过中断门（`int $T_SYSCALL`）陷入内核态的奇妙过程，理解了 `eax` 寄存器在传递系统调用号时的重要作用。在调度和内存管理方面，通过添加简单的打印日志，原本抽象的进程切换和物理页分配变得直观可见，我观察到了多个进程如何在一个 CPU 上交替执行，以及内存是如何被逐页分配出去的。最让我有成就感的是完成了生产者-消费者模型的构建。通过手动编写自旋锁和睡眠唤醒逻辑，我不仅掌握了操作系统内核中解决并发冲突的经典方法，还体会到了设计安全同步机制的挑战性。这次实验让我从一个操作系统的“使用者”逐渐向“构建者”转变。